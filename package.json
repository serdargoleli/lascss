{
  "name": "las-css",
  "version": "1.0.9",
  "description": "LAS CSS, Lightweight, modern, utility-first CSS framework built with SCSS for fast, responsive, and maintainable web design. Ideal for building responsive layouts without JavaScript.",
  "main": "dist/style.min.css",
  "style": "dist/style.min.css",
  "types": "index.d.ts",
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "import": "./dist/style.min.css",
      "require": "./dist/style.min.css",
      "default": "./dist/style.min.css"
    },
    "./style.css": "./dist/style.min.css",
    "./style.min.css": "./dist/style.min.css",
    "./dist/*": "./dist/*",
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "index.d.ts",
    "README.md",
    "LICENSE"
  ],
  "type": "commonjs",
  "sideEffects": [
    "**/*.css",
    "**/*.scss"
  ],
  "scripts": {
    "build": "npm run clean && npm run build:base && npm run build:utility && npm run build:meta && npm run build:main && npm run copy:assets",
    "build:base": "sass src/base.scss dist/base.min.css --style compressed --no-source-map",
    "build:meta": "sass src/meta.scss dist/meta.min.css --style compressed --no-source-map",
    "build:main": "sass src/main.scss dist/style.min.css --style compressed --no-source-map",
    "build:utility": "sass src/utility.scss dist/utility.min.css --style compressed --no-source-map",
    "copy:assets": "cp README.md dist/ && cp LICENSE dist/",
    "clean": "rm -rf dist && mkdir -p dist",
    "watch": "sass --watch src/main.scss dist/style.min.css --style compressed",
    "dev": "npm run watch"
  },
  "keywords": [
    "css",
    "scss",
    "utility-first",
    "responsive",
    "modern-css-framework",
    "web-design",
    "frontend-framework",
    "lightweight-css",
    "modular-css",
    "maintainable-css",
    "flexbox",
    "grid",
    "las-css"
  ],
  "author": "Serdar GOLELI",
  "license": "MIT",
  "devDependencies": {
    "sass": "^1.94.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  },
  "homepage": "https://github.com/serdargoleli/las-ui#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/serdargoleli/las-ui.git"
  },
  "bugs": {
    "url": "https://github.com/serdargoleli/las-ui/issues"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}